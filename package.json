{
  "name": "mongoose-redis-cache",
  "version": "0.0.0",
  "description": "Cache Mongoose MongoDB requests in Redis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "BSD"
}
